{
  "name": "tone-midi-player",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "NODE_ENV=production pnpm run build && touch dist/.nojekyll",
    "deploy": "gh-pages -d dist --dotfiles"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^7.1.7"
  },
  "dependencies": {
    "@tonejs/midi": "^2.0.28",
    "tone": "^15.1.22"
  }
}
